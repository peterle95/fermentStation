# Domain Docs

This repository uses a single-context layout.

Before exploring, read `CONTEXT.md` if it exists and relevant ADRs under `docs/adr/`. Missing files are expected until the domain is established.
