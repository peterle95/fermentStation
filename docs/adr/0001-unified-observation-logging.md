# Unified observation logging

Batch observation creation uses one adaptive logger above the timeline, while the timeline remains the history and management surface for existing entries. Temperature readings are stored as dedicated Celsius-backed observations rather than free text, so historical values remain valid and display can follow the user's unit preference without confusing them with the batch's current profile input.
