# FermentStation

Local-first household fermentation tracking for Android and desktop.

## Development

```powershell
npm install
npm run dev
```

```powershell
npm test
npm run build
npm run cap:sync
```

`npm run tauri:dev` requires Rust. Android builds require a JDK and Android SDK.
